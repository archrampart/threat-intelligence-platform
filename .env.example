# Threat Intelligence Platform - Docker Environment Variables

# Database Configuration
# For SQLite (default):
DATABASE_URL=sqlite:///./data/threat_intel.db

# For PostgreSQL (uncomment and use postgres profile):
# DATABASE_URL=postgresql://threat_intel:threat_intel_password@postgres:5432/threat_intel
# POSTGRES_DB=threat_intel
# POSTGRES_USER=threat_intel
# POSTGRES_PASSWORD=threat_intel_password

# Redis Configuration (automatically configured in docker-compose)
REDIS_URL=redis://redis:6379/0
REDIS_ENABLED=true

# Security Keys (CHANGE IN PRODUCTION!)
SECRET_KEY=your-secret-key-change-in-production
ENCRYPTION_KEY=your-32-byte-encryption-key-change-in-production!!

# Application Settings
ENVIRONMENT=production
LOG_LEVEL=INFO

# Watchlist Scheduler (set to true to enable automatic checking)
WATCHLIST_SCHEDULER_ENABLED=false

# API Keys (optional - can be set via UI)
# VIRUSTOTAL_API_KEY=your-api-key
# ABUSEIPDB_API_KEY=your-api-key
# OTX_API_KEY=your-api-key
